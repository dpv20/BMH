version: '3'

services:
  web:
    build: .
    command: gunicorn myproject.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DJANGO_SECRET_KEY= 'django-insecure-gl!-n1$8=le)wzu412vb%h&f#k-z6bsskxp11^m*ow^382v61q'
      - DJANGO_DEBUG = False